name: Links Checkerson: repository_dispatch: workflow_dispatch: schedule: - cron: '0 0 * * 1'# bi weekly on Mondays env: HUSKY: jobs: links-checker: runs-on: Ubuntu-latest name: Links Checker permissions: issues: write steps: - uses: actions/checkout@v4 - name: LinkChecker id: lychee uses: lycheverse/lychee-action @v2. 0. 2 with: args: --config./lychee .toml--verbose--no-progressREADME.md fail: true - name: CreateIssueFromFile if: env .exit_code!=0 uses: peter -evans /create -issue -from -file @v5 with: title: LinkCheckReport content-filepath :./lyche -out .md labels: report ,automated issue
